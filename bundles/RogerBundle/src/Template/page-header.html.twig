<h1>
	{{ title }}
	{% if add_button is defined and add_button == true %}
		<a href="#" onclick="{{ add_function | default('') }}" style="font-size: 24px;" class="roger-add-button">Add</a>
	{% endif %}
</h1>
